{
  "name": "md-reader",
  "version": "1.0.0",
  "description": "Local web app for browsing and rendering markdown files.",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check server.js && node --check lib/plugin-manager.js && node --check public/app.js && node --check public/plugins.js && node --check scripts/test-plugin-system.mjs",
    "start": "node server.js",
    "test": "node scripts/test-plugin-system.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "marked": "^18.0.1"
  }
}
